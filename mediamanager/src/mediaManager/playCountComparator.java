package mediaManager;
import java.util.Comparator;

public class playCountComparator implements Comparator<Integer>{
	public int compare(Integer a, Integer b) {
		//return (a.getName().compareTo(b.getName()));
	return 0;
	}
}


